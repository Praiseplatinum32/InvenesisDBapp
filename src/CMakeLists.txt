# Source modules CMakeLists.txt
# Note: Individual modules are now added directly from root CMakeLists.txt
# to ensure proper dependency ordering. This file is kept for potential
# future source-level configuration that applies to all modules.